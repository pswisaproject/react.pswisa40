import { makeConfig } from './config.common';

const config = makeConfig({
  DOMAIN: 'api.pswisa40',
  TITLE: 'PSW-ISA Project Team 40',
  API_TIMEOUT: 30000,
  // BASE_URL: 'http://api.pswisa40/'
});

export default config;
