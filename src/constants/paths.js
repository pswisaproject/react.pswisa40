const nonAuthenticatedRoutes = {
    INDEX: '/',
    HOME: '/home',
    LOGIN: '/login',
    REGISTER: '/register'
  };
  
  const authenticatedRoutes = {
    LOGOUT: '/logout'
  }
  
  export {
    nonAuthenticatedRoutes,
    authenticatedRoutes
  }
  